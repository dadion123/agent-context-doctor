# Claude Notes

Read `AGENTS.md` first. This file is a thin Claude Code adapter so the repository has one canonical agent context.

Claude-specific guidance:

- Keep implementation changes deterministic and test-backed.
- Prefer concise explanations in Japanese for Japanese users.
- Treat `AGENTS.md` as the source of shared commands and safety rules.
